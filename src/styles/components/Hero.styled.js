import styled from 'styled-components'

export const StyledHero = styled.section`
  margin: 2em auto 1em;
  max-width: 380px;

  h1 {
    font-family: 'LimeLight';
    color: var(--primary);
  }

  p {
    text-align: left;
  }

  .hidden {
    display: none;
  }
`
